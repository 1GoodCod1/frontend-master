/** Re-export from unified statusColors. Reports use fixed colors (no theme). */
export { getReportStatusColor as getStatusColor, getReportStatusBgColor as getStatusBgColor } from '@/utils/statusColors';
