/** Shared accent color used across MasterCard, recommendations, etc. */
export const ACCENT = '#E97525';
export const ACCENT_LIGHT = '#f08a3d';

/** Z-index scale — single source of truth for stacking contexts. */
export const Z_INDEX = {
  DROPDOWN: 50,
  LIGHTBOX_OVERLAY: 60,
  LIGHTBOX_CONTENT: 61,
} as const;
